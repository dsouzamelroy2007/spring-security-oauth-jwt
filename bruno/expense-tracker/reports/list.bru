meta {
  name: List reports (collection filtering, @PostFilter)
  type: http
  seq: 4
}

get {
  url: {{resourceServer}}/api/v1/reports
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
